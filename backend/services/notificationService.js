const sendAlertNotification = (alert) => {
    // Logic to send notification (email/SMS) can be implemented here
    console.log('Alert Notification:', alert);
};

module.exports = { sendAlertNotification };
